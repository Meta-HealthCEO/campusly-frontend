/** Phone bottom-nav tab classes. Other portals keep their original sizing; teacher section tabs share the width equally. */
const TAB_BASE = 'flex min-h-11 flex-col items-center justify-center gap-0.5 py-2 text-xs transition-colors';

export const FLAT_TAB_CLASS = 'flex min-h-11 flex-col items-center justify-center gap-0.5 px-3 py-2 text-xs transition-colors';

export const SECTION_TAB_CLASS = `${TAB_BASE} flex-1 px-2`;
